# Direct Provider Migration Status

This workspace uses PHENOVA-native names for the direct provider planning interface:
- `DirectProviderClient`
- `DirectProviderModelRouter`

The legacy file paths are retained temporarily for compatibility with existing workspace imports. Runtime integration, dependency installation, compilation, live provider calls, and render integration still require verification in a network-enabled build environment.
