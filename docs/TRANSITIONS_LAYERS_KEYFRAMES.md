# Transitions, layers and keyframes

Added validated operation primitives for:
- transitions
- layered media placement
- property keyframes

These operations define canonical Edit Plan data. They do not claim to render every
effect merely because a control exists. Actual media rendering must be implemented
and verified by the canonical executor for each supported operation.
