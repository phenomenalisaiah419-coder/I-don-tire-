fastapi>=0.110
uvicorn[standard]>=0.27
sqlalchemy>=2.0
pydantic>=2.0
pydantic-settings>=2.0
python-multipart>=0.0.9
PyJWT>=2.8
email-validator>=2.1
httpx>=0.27
pytest>=8.0

google-cloud-speech

# Optional — only needed for audio.beat_analysis (backend/app/services/audio_beat_analysis.py).
# The rest of the app runs fine without it; that capability reports itself as
# "planned" instead of "implemented" in /api/v1/capabilities if it's absent.
librosa>=0.10
